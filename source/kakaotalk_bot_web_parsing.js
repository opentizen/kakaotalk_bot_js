function response(room, msg, sender, isGroupChat, replier, imageDB) 
{
    if (msg=="message command reply") 
    // if (msg.indexOf("message command reply")==0) 
    // if (msg.indexOf("message command reply")!=-1)
{
    replier.reply // kakaotalk reply variable
    (Utils.getWebText("parsing URL") // use variable get url html source tag 
    .split("parsing html tag source part name") // use variable html source tag sort 
    .replace(/(<([^>]+)>)/g,"")); // use variable unnecessary remove html source tag
}
}